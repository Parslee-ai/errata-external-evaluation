# Lane-A v0.6.1 pre-agent stopped record

The v0.6.1 evaluation stopped before slot 3 reservation. Its freeze compared
the complete merged `codex --version` output: the restricted freeze environment
prefixed the stable version with an incidental PATH-alias warning, while the
required execution environment did not. The executable SHA-256 and terminal
`codex-cli 0.152.0` line matched. Verification failed closed before workspace
extraction, case prompt construction, model invocation, or ledger reservation.

Only the first selected case's oracle and incumbent rows completed. Both were
outcome wins with zero unauthorized actions and frozen-budget compliance. No
agent or derived-control row was reserved. No raw attempt was consumed; the
global count remains two of twelve.

- ledger terminal digest:
  `91da098a0663be1228e3e8e13ebacfbdf92da381812299d12d008478d8628324`
- ledger file SHA-256:
  `eb005846a491d497728f45ddd531e4f86ee9d731391f83249ec82785e39fc255`
- stopped-result file SHA-256:
  `d14a26297df4440167aca6c1c0dd6dce28eb15f87576d54686782738e2249f82`
- replay bundle SHA-256:
  `a9d897d871dcfa0f716757ec4c50ded0531a2ced53e253e172acf96dc659247c`

The cohort is unscored and is not a capability result. Because no candidate
cognition received case information, the successor retains the exact immutable
v0.6.2 selection without replacement or redraw. Its only repair normalizes the
stable version line while continuing to bind the executable bytes and SHA-256.
