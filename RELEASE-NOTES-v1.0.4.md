# v1.0.4 - SWE-bench-Live four-case pool closure

This immutable release closes the v1.0 real-software pool before cognition.
It is selection and custody evidence, not an agent result or capability claim.

The frozen NIST ranking was scanned through rank 34. Four outsider-authored,
repository-distinct cases met every prospective gate: three clean official
gold-patch resolutions, an empty-patch failure on at least one exact target
check, no preserved-test failure, and execution within the frozen host limit.

The retained cases are:

1. rank 9, `langchain4j__langchain4j-5792`;
2. rank 26, `colbymchenry__codegraph-749`;
3. rank 31, `modelcontextprotocol__go-sdk-862`; and
4. rank 34, `haraka__Haraka-3535`.

The closure retains all 34 scan dispositions and commits 120 preflight
artifacts whose bytes were verified equal across the two persistent custody
roots. Among the 18 dynamically evaluated rows, four qualified, seven failed
the official gold evaluator, four passed gold but failed the empty-patch
trap gate, one exceeded the frozen host limit, and two ended in image
registration failure. No rejected row was retried or replaced.

At closure, candidate invocations and raw attempt reservations are both zero.
The next admissible event is execution of the four frozen candidate rows as
global raw attempts 5 through 8. No case substitution or protocol repair is
permitted.

Key digests:

- closure file SHA-256:
  `b155698fbb58eb9e269fb33b98c9e7939f083eb65cbcb10cc21d14e87ee42efc`
- closure internal SHA-256:
  `61eabc9e6bcb7ad2b959f14270d6d61e6d8b2bd6bfcdd0e9c0de60f6f41fa7e5`
- preflight manifest SHA-256:
  `f37a921768f163d2f442d274ab6a1f9c818c47a9f7e717a97392fac0466636c2`

Reproduce the disclosure-safe closure from access to both custody roots with
`close_swebench_live_v100_pool.py`. The output is byte-identical and does not
print task statements, patches, hidden checks, or accepted solutions.
