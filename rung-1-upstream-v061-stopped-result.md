# Lane-A v0.6.1 pre-agent stop

## Decision

The v0.6.1 cohort is stopped before its first agent or derived-control row. It
is not scored and is not a capability result. Its immutable v0.6.2 case draw is
retained without replacement or redraw for the successor because no candidate
cognition received case information.

## Observed rows

Only the first selected case's independently computed oracle and incumbent rows
completed. Both reported `outcome-win`, zero unauthorized actions, and frozen
budget compliance. The append-only ledger ends at sequence 5 with digest
`91da098a0663be1228e3e8e13ebacfbdf92da381812299d12d008478d8628324`.
Its file SHA-256 is
`eb005846a491d497728f45ddd531e4f86ee9d731391f83249ec82785e39fc255`.

Before slot 3 reservation, candidate-freeze verification compared the complete
merged output of `codex --version`. The frozen, restricted environment prefixed
the stable version with an incidental PATH-alias warning; the execution
environment did not. The executable SHA-256 and terminal version line were
identical. Verification failed closed before workspace extraction, case prompt
construction, model invocation, or ledger reservation.

The first attempted NIST endpoint for the invalid v0.6.0 package also returned
HTTP 404 before response bytes and is not an evaluation row. No raw attempt was
consumed by either infrastructure stop. The global raw-attempt count remains
two, leaving ten.

## Smallest recovery

Normalize runtime version identity to the last nonempty version line while
continuing to bind the complete executable bytes and SHA-256. A regression uses
a fake executable that emits a diagnostic on stderr before its stable version.
The actual restricted and execution environments both normalize to
`codex-cli 0.152.0` while retaining executable SHA-256
`e642db09ae422ea96f043247f9df7f8936a1fba123cd9079752723a517311c9c`.

The successor changes no cognition, prompt, tool schema, authority, case,
briefing, outcome verifier, control, budget, threshold, or selected order. It
must receive a new immutable candidate freeze and a new append-only ledger.
The retained v0.6.2 selection digest is
`b940d6fef3972e3918d499118a3c3d096038f8ed29cc65a7691555a3cd4f7e52`.

The replay bundle contains the freeze, pool, qualification, briefings,
environment preflight, beacon receipt, draw, ledger, and both completed result
files. Its SHA-256 is
`a9d897d871dcfa0f716757ec4c50ded0531a2ced53e253e172acf96dc659247c`.
