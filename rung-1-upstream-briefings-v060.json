{
  "pallets--click--issue-3571": "The required behavior is that a progress bar reaches and renders its exact final position when update_min_steps does not divide the total, whether driven by iteration or explicit updates. Existing percentage, timing, and threshold behavior must remain intact. This states observable requirements only and does not identify an implementation action.",
  "pallets--click--issue-3572": "The required behavior is that confirm and prompt strip ANSI styling when color output is disabled and retain it when color output is enabled, matching echo behavior. Existing prompt input, defaults, and output routing must remain intact. This states observable requirements only and does not identify an implementation action.",
  "pallets--click--issue-3802": "The required behavior is that a late KeyboardInterrupt while Click reports an abort, error, or successful exit cannot replace the already determined exit code with an unhandled traceback. Non-standalone propagation and established abort messages must remain intact. This states observable requirements only and does not identify an implementation action.",
  "pydantic--pydantic--issue-13645": "The required behavior is that schema generation accepts unhashable generic aliases, including a PEP 695 alias parameterized by a ParamSpec list, instead of raising TypeError. Existing deprecated-alias handling and ordinary generic schemas must remain intact. This states observable requirements only and does not identify an implementation action.",
  "pydantic--pydantic--issue-13664": "The required behavior is that serialization-mode JSON Schema for date, datetime, time, and timedelta reflects ser_json_temporal and agrees with actual serialized output, including precedence over the legacy timedelta setting. Validation schemas and ISO formats must remain intact. This states observable requirements only and does not identify an implementation action.",
  "pydantic--pydantic--issue-13687": "The required behavior is that validate_call and generated argument schemas honor populate_by_name equivalently to enabled name and alias validation. Explicit runtime alias and name settings and ordinary BaseModel behavior must remain intact. This states observable requirements only and does not identify an implementation action.",
  "pydantic--pydantic--issue-13692": "The required behavior is that an unvalidated None default on a SecretStr field serializes as JSON null instead of raising an internal AttributeError, while actual secret values remain masked and optional-secret behavior remains intact. This states observable requirements only and does not identify an implementation action.",
  "pytest-dev--pytest--issue-14864": "The required behavior is that samefile comparison treats a zero Windows file ID as unknown rather than concluding that distinct paths are the same, so selecting one file on such a filesystem does not collect the whole suite. Real file-ID and path-equality behavior must remain intact. This states observable requirements only and does not identify an implementation action."
}
